{
  "name": "soundcloud-like-ratio",
  "version": "1.1.1",
  "private": true,
  "description": "Development tooling for the SoundCloud Like Ratio browser extension.",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "lint": "node --check content.js && node --check scripts/extension-files.js && node --check scripts/lint-extension.js && node --check scripts/package-extension.js && node --check scripts/verify-package.js && node --check scripts/verify-release-tag.js && node scripts/lint-extension.js",
    "build": "node scripts/package-extension.js",
    "verify:package": "node scripts/verify-package.js",
    "check": "npm run lint && npm test && npm run build && npm run verify:package",
    "start:chrome": "web-ext run --target chromium --start-url https://soundcloud.com --ignore-files AGENTS.md README.md package.json package-lock.json \"scripts/**\" \"tests/**\"",
    "start:firefox": "web-ext run --start-url https://soundcloud.com --ignore-files AGENTS.md README.md package.json package-lock.json \"scripts/**\" \"tests/**\""
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "jsdom": "30.0.1",
    "web-ext": "10.5.0"
  },
  "overrides": {
    "adm-zip": "0.6.0",
    "shell-quote": "1.10.0"
  }
}
